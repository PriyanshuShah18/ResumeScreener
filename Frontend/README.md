# Frontend

React + JavaScript UI for the HR resume screening workflow.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

Copy `.env.example` to `.env` and set:

- `VITE_API_BASE_URL`: backend API base URL (default `http://localhost:8000`)
